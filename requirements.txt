# Web Framework (pinned - auth middleware depends on starlette 0.35.x behavior)
fastapi==0.109.0
uvicorn[standard]==0.27.0
starlette>=0.35.0,<0.36.0
python-multipart>=0.0.9

# SSH & System
paramiko>=3.4.0
asyncssh>=2.14.2

# OpenSearch
opensearch-py>=2.4.2

# Data & Async
pydantic>=2.5.3
pydantic-settings>=2.5.2
aiohttp>=3.9.1

# Scheduling
apscheduler>=3.10.4

# Authentication (pinned - newer versions may change jwt.decode behavior)
PyJWT==2.8.0
passlib[bcrypt]>=1.7.4

# MCP (Model Context Protocol)
mcp>=1.9.0

# Utilities
python-dateutil>=2.8.2
pyyaml>=6.0.1
structlog>=24.1.0

# Vector similarity (optional — error detector falls back to hashing if unavailable)
zvec>=0.3.0
